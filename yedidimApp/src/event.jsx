import { react, useState } from 'react';

function event({ user, location, description, priority }) {

    return (
        <>
            <h1>Event !</h1>
            <p>User: {event.user}</p>
            <p>Location: {event.location}</p>
            <p>Description: {event.description}</p>
            <p>priority: {event.priority}</p>
        </>
    )
}


